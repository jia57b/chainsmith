# Sei Multi-Validator Localnet Environment Variables
# Copy to .env.multinode and adjust as needed

# Sei Docker image tag (built from source, defaults to "local")
SEI_TAG=local

# CometBFT persistent peers (auto-populated by init-multinode.sh)
PERSISTENT_PEERS=""

# Logging
LOG_LEVEL=info
