# Kii Multi-Validator Localnet Environment Variables
# Copy to .env.multinode and adjust as needed

# Kii Docker image tag (built from source, defaults to "latest")
KII_TAG=latest

# CometBFT persistent peers (auto-populated by init-multinode.sh)
PERSISTENT_PEERS=""

# Logging
LOG_LEVEL=info
