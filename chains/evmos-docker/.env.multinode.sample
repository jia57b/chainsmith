# Evmos Multi-Validator Localnet Environment Variables
# Copy to .env.multinode and adjust as needed

# Evmos Docker image tag
EVMOS_TAG=v20.0.0

# CometBFT persistent peers (auto-populated by init-multinode.sh)
PERSISTENT_PEERS=""

# Logging
LOG_LEVEL=info
